import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'reactiveFormDemo',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
